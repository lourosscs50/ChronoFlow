# ChronoFlow

.NET solution for events, identity, and **control automation** orchestration.

## Documentation

- **[Wave 2 — Control operators & release](docs/WAVE2_CONTROL_OPERATORS_AND_RELEASE.md)** — HTTP surface for control triggers and execution records, authentication, pending review actions, PostgreSQL migrations guidance, and **process-local dedupe** limitations.

## Development

Open `ChronoFlow.slnx` in your IDE. Run `dotnet restore`, `dotnet build`, and `dotnet test` from the repository root.
